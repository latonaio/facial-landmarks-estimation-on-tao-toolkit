version: "3"
services:
  faciallandmarks-tao-toolkit:
    container_name: faciallandmarks-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
